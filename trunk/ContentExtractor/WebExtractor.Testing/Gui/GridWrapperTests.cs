using System;
using System.Collections.Generic;
using System.Text;
using NUnit.Framework;
using ContentExtractor.Gui;
using ContentExtractor.Core;
using MetaTech.Library;
using System.Windows.Forms;
using System.Drawing;
using System.Xml;

namespace WebExtractor_Testing.Gui
{
  [TestFixture]
  public class GridWrapperTests 
  {
    [SetUp]
    public void SetUp()
    {
    }

    //private DataGridView.HitTestInfo InfoFromCell(int column, int row)
    //{
    //  //Rectangle rect = view.DataGrid.GetCellDisplayRectangle(column, row, false);
    //  //DataGridView.HitTestInfo info = view.DataGrid.HitTest(rect.X + rect.Width / 2, rect.Y + rect.Height / 2);
    //  //return info;
    //}

    [Test]
    public void AddOneXmlNode()
    {
    }
  }
}
